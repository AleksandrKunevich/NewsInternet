<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/itemNewsContainer"
    android:layout_width="match_parent"
    android:background="@color/teal_200"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imageItemNews"
        android:layout_width="84dp"
        android:layout_height="84dp"
        android:layout_gravity="start"
        android:layout_margin="8dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_baseline_error_84"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

    </ImageView>

    <TextView
        android:id="@+id/textViewItemNews"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:textColor="@color/black"
        android:textSize="18sp"
        app:layout_constraintEnd_toStartOf="@id/isSaveNewsCheckBox"
        app:layout_constraintStart_toEndOf="@id/imageItemNews"
        app:layout_constraintTop_toTopOf="parent">

    </TextView>

    <CheckBox
        android:id="@+id/isSaveNewsCheckBox"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="start"
        android:layout_margin="8dp"
        android:button="@drawable/custom_checkbox_item_news"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

    </CheckBox>

</androidx.constraintlayout.widget.ConstraintLayout>